# Pinned loosely: Colab ships recent torch and opencv builds, and pinning them
# exactly would force a slow reinstall at the start of every session.
torch>=2.1
torchvision>=0.16
segmentation-models-pytorch>=0.3.3
albumentations>=1.4
opencv-python>=4.8
pycocotools>=2.0.7
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
matplotlib>=3.7
kaggle>=1.6
